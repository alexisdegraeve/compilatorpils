/*

0

0,0,0

0,0,0,0,0
*/
declare and retain
    b as boolean;
    i as integer;
    s as square[5];
    s2 as square[5,5];
when your turn
by default
    do
        set s2[0 + 2, 3 + 1] to zombie
        set s[0] to player
        set s[0] to ennemi
        set s[0] to map
        set s[0] to radio
        set s2[0, 4] to ammo
        set s[0] to fruits
        set s[0] to soda
        set s[0] to nearby[2,2]
    done