/* My comments */
declare and retain
   f as integer;
	facto as function(x as integer):integer
    	do
    		if x < 0 or x = 0 then
    			set f to 1
    		else
    			set f to x * facto(x - 1)
    		done
    	return f
    	done
    i3 as integer;
when your turn
by default
    do
    	set i3 to facto(5)
    	if not i3 =  v2[5+2] * 4 * 3 * 2 then
    		next move west
    	done
    	next do nothing
    done


    set i2 to 6
    set i3 to 7
    set t to i2 = i3