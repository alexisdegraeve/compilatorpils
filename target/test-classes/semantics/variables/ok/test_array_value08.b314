/* My comments */
declare and retain
    b as boolean;
    i as integer;
    s as square;
when your turn
by default
    do
        compute nearby[2 + 3 - 42 * 0, 2 + 3 - (42  % 5) * 0]
        compute nearby[2 + i - 1, 2 + 3]
        compute nearby[2 + i - 1, i]
        compute nearby[i+4 - 1, 4 - i]
    done