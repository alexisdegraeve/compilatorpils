/* My comments */
declare and retain
	b1 as boolean;
	b2 as boolean;
	b3 as boolean;
	f1 as integer;
    i1 as integer;
	b4 as boolean;
	b5 as boolean;
	s1 as square;
	i2 as integer;
	b6 as boolean;
	v2 as boolean[5, 7];
    v3 as integer[42, 58];
    v4 as integer[42];
    i3 as integer;
    s2 as square;
    f3 as boolean;

    fct2 as function(x as integer, y as boolean):integer
    	do
    		set x to 10
    	return x
    	done

    fct1 as function():square
    	do
    		compute i1 + fct2(1, true)
    		set i1 to i1 + 1 % longitude + grid size - i1
    		set f1 to i1
    	return dirt
    	done

    fct3 as function():boolean
       declare local
          t as integer;
    	do
    		set f3 to true and false or not graal is north or b1 and b2
    	return f3
    	done
    fct4 as function():square
    	do
    		skip
    	return s2
    	done

when your turn
by default
    do
skip
    done