declare and retain
	b1 as boolean;
	b2 as boolean;
    i1 as integer;
    i2 as integer;

    fct1 as function():boolean
		declare local
			loc1 as integer;
			loc2 as boolean;
			loc3 as square;
			loc4 as integer[5];
			f1 as boolean;
    	do
    		set loc2 to true
    		set loc1 to 34
    		set f1 to loc2 and loc1 < 42
    	return f1
    	done

when your turn

	when false do
		next move west
	done

	when fct1() do
		next move north
	done

	by default do
		next do nothing
	done